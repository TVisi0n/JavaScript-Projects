var Sent1 = "This is the beginning of the string" // Assigning 2 variables
var Sent2 = " and this is the end of the string."

document.write(Sent1 + Sent2) // Using doc.write method and calling the 2 variables I set above and concatenating them

window.alert(3 + 4) // Expression example